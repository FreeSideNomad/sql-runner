package com.ivamare.domain;

/** Supported parameter types for query parameters. */
public enum ParameterType {
  STRING,
  INTEGER,
  DECIMAL,
  DATE,
  DATETIME,
  BOOLEAN,
  ENUM,
  LIST_STRING,
  LIST_INTEGER
}
